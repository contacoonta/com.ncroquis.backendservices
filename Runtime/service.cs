
using UnityEngine;

namespace Ncroquis.Backend
{
    public class BackendService : MonoBehaviour
    {
        void Start()
        {
            string version = "0.0.1";
            Debug.Log($"BackendService version : {version} 패키지가 실행됩니다!");
        }
    }
}
